#!/bin/bash
TARGET=`cat .target`
LINE=`ack version ../$TARGET/package.json`
VERSION=`echo $LINE | sed 's/^.*[^0-9]\([0-9]*\.[0-9]*\.[0-9]*\).*$/\1/'`
LAST=`cat .version`
PREFIX="--"
PWD=`pwd`
APP=`basename $PWD`

if [ "$VERSION" = "$LAST" ];
then
  if [ $# == 0 ]
  then
    printf "%s skip... [ %s ] \n" $PREFIX $VERSION
  fi
else
  printf "%s %s \n" $PREFIX $VERSION
  echo $VERSION > .version
  sed "s/VERSION/$VERSION/g" <.template.html >index.html

  git commit -a -m "bump $VERSION"
  git push
  vc --prod
  printf "%s %s %s.%s \n" $PREFIX "APP:" $APP "vercel.app"
fi
